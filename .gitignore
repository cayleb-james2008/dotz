node_modules/
dist/
release/
# local copies of shipped installers + latest.json (scripts/release.ps1, gh release download)
release-archive/
assets/models/
.env
.env.local
data/
*.log
# Test artifact: a hooks test command redirects to `> nul` (the Windows null device),
# which creates a real zero-byte file named `nul` when the suite runs on Linux.
**/nul
.DS_Store
.ai-agents/

# Solomon RSI host venv (interpreter only, created to satisfy run_improver launcher)
.venv/

# Rust build output
/target/
# Tauri generated (schemas/acl) — regenerated by tauri-build
/src-tauri/gen/
/src-tauri/permissions/
/src-tauri/target/
.dotz-revive-logs/
