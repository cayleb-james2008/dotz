{
  "name": "dotz",
  "version": "0.2.0",
  "private": true,
  "description": "dotz — a native Rust (axum + Tauri + ort) Claude-Code-style multi-agent coding dashboard: on-the-fly workflow graphs, unified skills, on-device memory, and a bento UI. (This package.json only supports the Rust build — it ships the agent-browser binary and fetches the ONNX embedding model.)",
  "type": "module",
  "scripts": {
    "fetch-model": "node scripts/fetch-embed-model.mjs"
  },
  "dependencies": {
    "agent-browser": "0.27.0"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.2.0"
  }
}
