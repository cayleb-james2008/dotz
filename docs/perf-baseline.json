{
  "cold_start_ms": 526,
  "measured_at": "2026-07-20",
  "host": "windows-latest (dev host, 16GB, bundled all-MiniLM-L6-v2 ONNX)",
  "metric": "Embedder::load() median (criterion, 10 samples, 20 iterations)",
  "note": "Captured via 'cargo bench --bench cold_start -- --save-baseline main' on commit 3dbe043. The bench measures the dominant cold-start cost (ONNX session + tokenizer bring-up). CI compares against this baseline with a 15% regression threshold; a PR that regresses cold-start beyond 15% fails the gate."
}